import type{Revision}from'../types/workflow';export function RevisionTimeline({items}:{items:Revision[]}){if(!items.length)return null;return <section className="glass rounded-2xl p-5"><p className="font-display font-semibold">Revision history</p><div className="mt-4 space-y-3">{items.map((r,i)=><div key={i} className="flex gap-3"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-amber/10 text-xs font-bold text-amber">{r.revision_number}</span><div><p className="text-sm capitalize">{r.agent_name} Agent revised</p><p className="mt-1 text-xs text-slate-500">{r.reason}</p></div></div>)}</div></section>}
