import{X}from'lucide-react';import type{AgentData}from'../types/workflow';import{StatusBadge}from'./StatusBadge';
export function AgentDetails({name,data,onClose}:{name:string;data?:AgentData;onClose:()=>void}){if(!name)return null;return <div className="fixed inset-0 z-50 flex justify-end bg-black/60 backdrop-blur-sm" onClick={onClose}><aside className="scrollbar h-full w-full max-w-lg overflow-y-auto border-l border-white/10 bg-[#0b151f] p-6 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between"><div><p className="text-[10px] uppercase tracking-[.25em] text-mint">Agent inspector</p><h2 className="mt-2 font-display text-2xl font-semibold capitalize">{name.replace('_',' ')} Agent</h2></div><button onClick={onClose} className="rounded-lg border border-white/10 p-2 text-slate-400"><X size={17}/></button></div><div className="mt-6 flex items-center gap-3"><StatusBadge status={data?.execution?.status||'IDLE'}/>{data?.execution?.duration_seconds!=null&&<span className="text-xs text-slate-500">{data.execution.duration_seconds.toFixed(2)}s</span>}</div>{data?.revisions?.map((r,i)=><div key={i} className="mt-5 rounded-xl border border-amber/20 bg-amber/5 p-4"><p className="text-xs font-semibold text-amber">Revision {r.revision_number}</p><p className="mt-2 text-xs leading-relaxed text-slate-300">{r.reason}</p></div>)}<div className="mt-6"><p className="mb-2 text-xs font-semibold uppercase tracking-wider text-slate-500">Current output</p><pre className="overflow-x-auto whitespace-pre-wrap rounded-xl border border-white/10 bg-black/30 p-4 text-xs leading-relaxed text-slate-300">{data?.output?JSON.stringify(data.output,null,2):'No output yet.'}</pre></div></aside></div>}
