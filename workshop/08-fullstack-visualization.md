# Lab 8 — Full-Stack Visualization

FastAPI starts work in the background. `EventService` publishes typed events to `/ws/workflows/{id}`. React reconnects, reconciles REST state, updates React Flow nodes, and exposes the final evidence dashboard.

Exercise: disconnect the browser network briefly during execution. Reconnect and verify history is recovered from the workflow status endpoint.
