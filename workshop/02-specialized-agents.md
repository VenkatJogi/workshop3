# Lab 2 — Specialized Agents

Assign architecture, cost, security, performance, and risk to separate agents. Read their focused prompts and output schemas in `backend/app/agents` and `backend/app/models/agent_outputs.py`.

Exercise: add Requirements Agent to a scenario with ambiguous constraints. Compare its unknowns with the original single-agent assumptions.
