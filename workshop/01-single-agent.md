# Lab 1 — Single Agent

Start with `one question → one Gemini call → one answer`. Ask the migration question and inspect how one response blends architecture, cost, and risk without ownership boundaries. Record missing evidence and contradictions. This establishes the baseline: fluent output is not an auditable workflow.

Exercise: call `GeminiService.generate_structured` with `DecisionOutput`, then list which claims need specialist validation.
