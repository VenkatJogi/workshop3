# Lab 4 — Planner Agent

The planner converts the brief into criteria, research questions, and a dynamic allow-listed agent selection. It cannot name arbitrary code because `PlannerOutput` validates the specialist registry.

Exercise: compare a security-policy decision and a performance-migration decision. Verify different specialist sets are selected.
